# Session Summary: 2026-08-27 - main

## Goals
- Close Findings 3 and 4 of `docs/context/mutation-audit-20260827.md` before starting Phase 17, security-relevant paths first (page content sanitizer, password verification).

## Phase Context
- **Plan**: `docs/work/mingle-ts-full-parity/plan.md` — "mingle-ts-full-parity" (Plan Status: ACTIVE)
- **Phase executed**: None — this session worked directly off the 2026-08-27 mutation audit's open findings, independent of the plan's phase sequence. Plan remains at Phase 16 DONE / Phase 17 PENDING; no phase status was changed this session.
- **Tool calls used**: 128 / 350 (budget carried from prior session setup; not phase-scoped work)
- **Phase outcome**: N/A — no phase was executed

## Completed

### Finding 3 CLOSED — changed-field event payload directional gap
- `identity/commands.server.ts:222,223` and `projects/commands.server.ts:280,281,282`: every existing test changed all fields of a record at once, so the `false`-direction mutant on each "did this field change" check was already dead; only the `true` direction survived because no fixture held one field still while another moved.
- 4 tests added: 2 in `test/identity.behavior.test.ts`, 2 in `test/projects.behavior.test.ts`, each holding one field constant to force the mixed-change case.

### Finding 4 CLOSED — four no-coverage regions
- `pages/content.server.ts` sanitizer: 7 tests in `test/page-content.behavior.test.ts` for the numeric-character-entity decoder (decimal, hex both cases, out-of-range codepoints, entities inside attributes decoded before the URL safety check) and comment/doctype/unterminated-tag skips.
- `identity/password.server.ts`: new `test/password.behavior.test.ts`, 9 tests.
- `cards/mql.server.ts`: 15 tests in `test/mql.behavior.test.ts` covering every parser refusal path, cross-kind property comparison, and formula comparability rules.
- `cards/mql-evaluator.server.ts`: 5 tests in `test/mql-filter.behavior.test.ts` covering predefined-column arms and the deliberate throws for MQL constructs no backing model can answer yet (tags, plans, card relationships, THIS CARD).

### Real defect found and fixed — password verification fail-open
- `app/domain/identity/password.server.ts`: `verifyPassword` returned `true` for **any** password against a stored hash whose final field was empty (`scrypt:16384:8:1:<salt>:`). `Buffer.from("", "hex")` is zero-length, so the derived key length was zero, `scryptSync` produced zero bytes, and `timingSafeEqual` reported two empty buffers equal — a fail-open in the one function whose doc comment promised the opposite.
- Fix: new `decodeHexField` helper refuses any field that is not complete, even-length hex (`Buffer.from` otherwise truncates silently at the first bad character), plus a `KEY_LENGTH` floor on the decoded hash. Reachable only via a corrupted or hand-edited `users.password_hash` row — `hashPassword` always emits 128 hex characters.

### Verification method
- Every added test was verified by applying the mutant it targets, confirming the test fails, then reverting — 40 tests added, 42 distinct mutant-kills individually verified, 13 mutants proved equivalent or unreachable across both audit passes.
- Suite 536 → 576 passing (excluding the live-stack `healthz.real` test in both counts), `npx tsc --noEmit` clean, `npm run build` clean (evidence: event log rows at 06:42, 07:12 — "18 passed 552/576 passed"; build-passed row at 06:51).
- Mutation re-run (`npm run test:mutation`, 18m16s, 5,967 mutants; evidence: event log build-passed row at 06:52): total score 84.28% → 87.11%, covered 87.20% → 88.57%, survived 734 → 670, no-coverage 199 → 98, runtime errors 4 → 7. Per-file: `password.server.ts` 59.09 → 82.00, `content.server.ts` 73.71 → 81.10, `mql-evaluator.server.ts` 73.04 → 79.94, `identity/commands.server.ts` 75.48 → 80.29.
- The 98 remaining no-coverage mutants are **not** a Finding 4 remainder — Finding 4 named 199 as a total and enumerated only three concentrations, all now closed; the remainder is spread thin across files the finding never listed and is untriaged.

### ADR-0013 written and ACCEPTED
- `docs/adrs/0013-password-verification-fails-closed-with-a-key-length-floor.md`: records the fail-closed verification contract, the `KEY_LENGTH` floor's forward-compat cost, the deliberate exclusion of the scrypt cost fields (N/r/p) from validation, and the user's rule-11 decision to keep mutation testing report-only rather than a CI gate.

### ADR review (/devarch:adr-review on ADR-0013) — three corrections
1. Decision headline overclaimed ("fails closed on every malformed stored value") while Decisions 1-3 govern only hex fields; measured that Node's `scryptSync` treats falsy cost options as absent and substitutes defaults, so `N=0`/`r=0`/`p=0`/`N=""` verify the correct password rather than being refused (not a bypass — wrong passwords still rejected). Headline narrowed, non-decision paragraph added.
2. The claim "raising `KEY_LENGTH` will not surface as a test failure" was unverified testimony; established it by raising `KEY_LENGTH` 64→128 in place and re-running (576/576 still green).
3. Three counting errors corrected in the audit doc and ADR: tests 36→40, mutant-kills 36→42, equivalent/unreachable 8→9 (12→13 across both passes), suite baseline 577→576 (the 536 baseline had excluded `healthz.real`).

## Key Decisions

### 1. Report-only mutation testing (ADR-0013)
User decided, when asked under rule 11, to keep `npm run test:mutation` a manual/report-only tool rather than wiring it into CI as a merge gate — recorded as an explicit non-decision in ADR-0013 rather than left implicit.

### 2. Fail-closed hex decoding over silent truncation
`Buffer.from(str, "hex")` truncates at the first invalid character instead of throwing, which is the mechanism that let an empty/malformed final field silently produce a zero-length key. `decodeHexField` rejects incomplete or odd-length hex explicitly rather than relying on downstream length checks alone.

## Next Phase
- **Phase 17**: "Macro framework and chart macros" — PENDING, Large tier (400 budget), awaiting explicit user go-ahead per rule 5.
- **Entry state**: Phase 16 pages exist; Phase 13 MQL evaluator exists.
- **Note**: Phase 17's macros render through `pages/content.server.ts`, the sanitizer this session hardened, and must obey ADR-0011's content-ordering invariant (sanitize-then-render).

## Open Items

### Short Term
- 98 untriaged no-coverage mutants (post-session count) are the honest starting point for a third mutation-audit pass; not yet enumerated by file/concentration.
- Runtime errors rose 4 → 7, all in `mql.server.ts` (mutants that crash rather than being killed or surviving) — not investigated this session.

### Long Term
- The standing `pattern-recurrence-detector` recommendation to institutionalize `npm run build` as a standing end-of-phase step is still not codified as a rule, though build was run and clean this session.

## Files Modified

**Source** (1 file):
- `mingle-ts/app/domain/identity/password.server.ts` - fail-open fix: `decodeHexField` helper + `KEY_LENGTH` floor

**Tests** (6 files, 1 new):
- `mingle-ts/test/password.behavior.test.ts` - new, 9 tests
- `mingle-ts/test/page-content.behavior.test.ts` - +7 tests, sanitizer entity/skip coverage
- `mingle-ts/test/mql.behavior.test.ts` - +15 tests, parser refusal paths
- `mingle-ts/test/mql-filter.behavior.test.ts` - +5 tests, evaluator no-coverage arms
- `mingle-ts/test/identity.behavior.test.ts` - +2 tests, changed-field directional coverage
- `mingle-ts/test/projects.behavior.test.ts` - +2 tests, changed-field directional coverage

**Docs** (2 files, 1 new):
- `docs/context/mutation-audit-20260827.md` - Findings 3 and 4 marked CLOSED, delta recorded, method notes added
- `docs/adrs/0013-password-verification-fails-closed-with-a-key-length-floor.md` - new, ACCEPTED

## Notes

**Session duration**: ~1.5 hours (06:34–07:22 UTC per session state).

**Approach**: Mutant-driven test derivation — for each surviving/no-coverage mutant in the audit's Finding 3/4 scope, applied the mutant, wrote a test that fails against it, confirmed the fail, reverted, confirmed the test still passes clean. The password fail-open was found as a byproduct of writing coverage for `verifyPassword`, not sought independently.

---

## Session Metadata

- **Status**: COMPLETE
- **Blocker** (if any): N/A
- **Blocker Category**: N/A
- **Estimated Remaining**: N/A
- **Rollback Safety**: safe to revert — all changes are additive tests plus one isolated source fix in `password.server.ts`; no schema or migration changes.

## Dependency/Prerequisite Check

- **Prerequisites met**: Findings 3 and 4 were pre-identified by the prior session's mutation audit (`docs/context/mutation-audit-20260827.md`, session 39e7a3); this session had a concrete, enumerated target list going in.
- **Prerequisites discovered**: None.

## Architectural Decisions

- ADR-0013 (new, ACCEPTED): password verification fails closed on incomplete/malformed hex fields via a `KEY_LENGTH` floor; scrypt cost fields (N/r/p) deliberately excluded from validation because Node's `scryptSync` already treats falsy values as "use default" rather than as a bypass; mutation testing stays report-only, not a CI gate (rule-11 decision).
- No pattern from a prior ADR was applied or revised beyond ADR-0011's ordering invariant, which this session's sanitizer tests exercise but do not change.

## Mutation Audit

- Files with state-changing logic modified: `app/domain/identity/password.server.ts` (`verifyPassword` — no persisted state change, but a security-critical boolean-returning guard; treated as state-changing for audit purposes since it gates authentication).
- Tests verify actual state mutations (not just events): YES (evidence: each of the 40 added tests was verified by applying its target mutant, observing the test fail, then reverting — event log rows at 06:42 "18 passed 552 passed" and 07:12 "18 passed 576 passed" corroborate the suite growth; mutation re-run event at 06:52 corroborates the score delta).
- If NO: N/A.

## Recurrence Check

- Similar to past issue? YES — `docs/context/mutation-audit-20260827.md` itself documents that the branch-coverage gap this session closes recurred across Phase 8, Phase 15, the 02:49 ADR-review session, and Phase 16 before being addressed systemically in the 2026-08-27 audit (session 39e7a3). This session is the follow-through on that audit's Findings 3 and 4, not a new recurrence.
- If YES: The audit already recommends the systemic fix (mutation testing as a standing, if report-only, verification step); no further one-time audit needed beyond the untriaged 98 no-coverage mutants noted above.

## Test Coverage Delta

- Tests added: 40 (evidence: suite count delta below; audit doc corrected count after ADR review, see Completed section item 3).
- Tests passing before: 536 → after: 576, both excluding `test/healthz.real.test.ts` (evidence: event log rows "18 passed 552 passed" at 06:42:33 UTC and "18 passed 576 passed" at 07:12:17 UTC, both timestamped after their respective edits).
- Known untested areas: 98 no-coverage mutants remaining post-session, spread across files outside Finding 4's three named concentrations, untriaged; runtime-error mutants (7, up from 4) in `mql.server.ts` not investigated.

---

**Progressive update**: Session completed 2026-08-27 07:22
