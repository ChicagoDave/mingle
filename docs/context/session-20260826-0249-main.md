# Session Summary: 2026-08-26 - main

## Goals
- Answer rule 11's outstanding ADR-worthy ask for the three Phase 15 candidate decisions (applyCardPropertyValue entry point, transition-only enforcement split, null-means-unset prerequisite semantics).
- Run multi-ADR review across the three and close its findings.
- No plan phase executed — Phase 16 remains PENDING, awaiting explicit go-ahead.

## Phase Context
- **Plan**: `docs/work/mingle-ts-full-parity/plan.md` — full feature-parity rewrite of Mingle in TypeScript.
- **Phase executed**: None — this session is ADR authoring and review follow-through on Phase 15 (DONE, previous session), not a plan phase.
- **Tool calls used**: 52 / 350 (Phase 12 budget row in state file; not meaningful here since no phase advanced).
- **Phase outcome**: N/A — no phase in progress this session.

## Completed

### Session start
- Recapped the Phase 15 session (`session-20260826-0119-phase15-bulk-transitions.md`); ran `pre-session-audit` — no blockers, type check clean, Phase 16 confirmed PENDING awaiting go-ahead, audit flagged the growing backlog of unanswered "ADR-worthy?" asks and the five-session `mutation-verification` first-pass-gap pattern. Project profile fresh; gate cleared.

### Three ADRs written (all ACCEPTED, no Open Questions)
- `docs/adrs/0008-applycardpropertyvalue-is-the-ui-property-change-entry-point.md` — `applyCardPropertyValue` is the card-UI property-change entry point, `setCardPropertyValue` the direct write; dispatcher composes and writes nothing itself (preserves ADR-0007 Decision 4's single-writer invariant); six-variant `AutoTransitionOutcome` with non-writing outcomes as values, not errors; `unchanged` checked before the transition-only branch; matching over available transitions only; every future property-writing caller (bulk set, import, REST Phase 30) must choose a command explicitly.
- `docs/adrs/0009-transition-only-enforcement-is-split-between-guard-and-dispatcher.md` — the direct write's transition-only guard is role-sensitive (project admin bypasses, legacy's exact message, fires only on a real change); the dispatcher routes on the flag for every actor regardless of role. Framed as authorization question vs. semantic question. Notes the admin bypass is currently unreachable from the browser.
- `docs/adrs/0010-null-has-specific-value-prerequisite-means-unset.md` — null prerequisite value means UNSET, blank rejected at definition time; mirrors ADR-0007 Decision 1's null-action-clears convention instead of adding a `has_no_value` kind; renders "(not set)"; carries the branch-coverage requirement and the Phase 30 import-mapping caveat.
- Updated the Phase 15 session summary's Key Decisions, Architectural Decisions, and Open Items sections to link the three ADRs and drop the stale "not yet answered / do not write ADR-0008" text.

### Multi-ADR review (`/devarch:adr-review 0008 0009 0010`)
- Combined verdict: READY WITH CLARIFICATIONS. All 5 cross-references resolved; 4 "stable across" claims verified against code; vocabulary consistent on 6 terms; sequencing acyclic with one mutual pair (0008↔0009); no Open Questions anywhere.
- Findings: (1) SMALL — ADR-0009's "no role test at all" contradicted the code (`applyCardPropertyValue` does enforce FULL_TEAM_MEMBER at entry) — fixed in text. (2) SMALL — the no-change case has two contracts (dispatcher returns `{ok:true, unchanged}`, direct write rejects with "has no changes to save") and neither ADR said so — fixed in text. (3) SMALL — ADR-0009 named admin-through-dispatcher as the case most likely to be "fixed" wrongly, but no test pinned it (all 13 dispatcher tests used memberId, one readonlyId, none projectAdminId) — closed with a new test. (4) DEFERRED — mutual dependency 0008↔0009, no action. (5) DEFERRED — "sole entry point" wording survives in the Phase 15 summary heading, no action.
- Note: the project's ADR template (Context/Decision/Consequences/Session) has no Acceptance Criteria or Test Requirements sections corpus-wide, so those review checklist items graded N/A by convention, not as per-ADR failures.

### Finding 3 closed with a new test
- `mingle-ts/test/transition-workflows.behavior.test.ts`, "routes a project admin through the transition too, instead of writing the value" (line ~1170) — asserts `transition_applied`, Status AND Owner both set (the transition's second action distinguishes the transition path from a direct write), version +1, `TransitionExecuted` +1, `CardPropertyValueSet` unchanged. Graded GREEN.
- Mutation-checked: temporarily added a PROJECT_ADMIN bypass to the dispatcher's routing branch — exactly the new test failed (1 failed | 40 passed on that file), mutant reverted via `git checkout`. ADR-0009 now cites the new test and records the mutation check inline.

## Key Decisions
See ADR-0008, ADR-0009, ADR-0010 above — all three were the subject of this session, not a new decision made in it.

## Next Phase
- **Phase 16**: "Wiki pages and rich editing" (Large, budget 350) — `Page` aggregate with `page_versions` mirroring `card_versions`, TipTap rich text, cross-page and page-to-card linking, per `docs/work/mingle-ts-full-parity/plan.md`. Still PENDING; not advanced to CURRENT — awaiting explicit user go-ahead.
- **Entry state**: Phase 5's versioning pattern exists as the template to reuse (satisfied).

## Open Items

### Short Term
- Phase 16 awaits go-ahead: wiki pages and rich editing.
- Phase 11's favorites-schema ADR-worthy ask is still unanswered.
- Legacy's hidden "Moved to `<Value>` on" date property remains deferred (no `hidden` column on `property_definitions` yet; cross-cutting through list columns/grid/filters/card page).
- Carried forward from Phase 14/15: `require_comment` (Phase 20 comments), project-variable bindings on prerequisites/actions, tree actions (Phase 23), transition edit-in-place (delete+recreate only), transition usage checks on card-type change or property-value rename.

### Long Term
- Attachments do not bump card version.
- A property rename must rewrite formula/MQL texts referencing the old name.
- `devarch activate team` capability suggestion still pending.
- The Phase 8 recurrence-check recommendation on Behavior-Statement branch coverage remains largely unacted-on — this session partially addresses the same class of gap by adding the missing admin/dispatcher branch test (ADR-0009 finding 3), but the broader recommendation is not closed out.

## Files Modified

**New** (3 files):
- `docs/adrs/0008-applycardpropertyvalue-is-the-ui-property-change-entry-point.md` - ADR, ACCEPTED
- `docs/adrs/0009-transition-only-enforcement-is-split-between-guard-and-dispatcher.md` - ADR, ACCEPTED
- `docs/adrs/0010-null-has-specific-value-prerequisite-means-unset.md` - ADR, ACCEPTED

**Modified** (2 files):
- `docs/context/session-20260826-0119-phase15-bulk-transitions.md` - linked the three ADRs into Key Decisions/Architectural Decisions/Open Items, removed stale "not yet answered" text
- `mingle-ts/test/transition-workflows.behavior.test.ts` - added the admin-through-dispatcher regression test (finding 3)

No `app/` source files remain modified — the mutation-check mutant was reverted via `git checkout` (confirmed via `git status --short` showing no `app/` changes).

## Notes

**Session duration**: ~20 minutes (02:49–03:08 CDT).

**Approach**: Answer the standing ADR-worthy ask first (three ADRs), then run multi-ADR review to catch cross-seam issues per-ADR review would miss, then fix the two SMALL text findings inline and close the one test-gap finding with a real regression test verified by mutation, rather than by inspection alone.

---

## Session Metadata

- **Status**: COMPLETE
- **Blocker**: N/A
- **Blocker Category**: N/A
- **Estimated Remaining**: N/A
- **Rollback Safety**: safe to revert

## Dependency/Prerequisite Check

- **Prerequisites met**: Phase 15 DONE (three candidate decisions existed to write up); ADR-0007 Decisions 1 and 4 existed as the conventions ADR-0008/0009/0010 needed to stay consistent with.
- **Prerequisites discovered**: None.

## Architectural Decisions

- ADR-0008: `applyCardPropertyValue` is the UI property-change entry point, `setCardPropertyValue` the direct write — keeps the single-writer invariant (ADR-0007 Decision 4) and forces every future property-writing caller to choose a command explicitly.
- ADR-0009: transition-only enforcement is split by design — role-sensitive guard on the direct write, flag-only routing in the dispatcher — because the two enforce different things (authorization vs. semantics).
- ADR-0010: null `has_specific_value` means UNSET (mirrors ADR-0007 Decision 1's null-clears convention); blank is rejected at definition time.
- Pattern applied: multi-ADR seam review (`/devarch:adr-review`) before treating a batch of related ADRs as closed.

## Mutation Audit

- Files with state-changing logic modified: none remain modified — the dispatcher mutant (PROJECT_ADMIN bypass added to `transitions.server.ts`'s routing branch) was applied, confirmed to kill the new test, then reverted via `git checkout`.
- Tests verify actual state mutations (not just events): YES (evidence: mutant applied → "1 failed | 40 passed" on `transition-workflows.behavior.test.ts`, exactly the new admin-routing test failing; mutant reverted; clean run afterward confirmed by the 41-passed single-file row below).
- If NO: N/A.

## Recurrence Check

- Similar to past issue? YES — the Phase 8 session's recurrence check already flagged a Behavior-Statement branch-coverage gap pattern, and this session's finding 3 (an untested admin/dispatcher branch caught only by ADR review, not by the original test suite) is another instance of the same class.
- Consider a one-time audit of Behavior Statement branch coverage across the `transitions.server.ts` / `transition-workflows.server.ts` command set, as previously recommended and still open.

## Test Coverage Delta

- Tests added: 1 (`transition-workflows.behavior.test.ts`, "routes a project admin through the transition too, instead of writing the value").
- Tests passing before: 429/429 (Phase 15 baseline) → after: 430 total, 429 passed, 1 failed (evidence: event log `.devarch-events-3076c1.jsonl`, `2026-08-26T08:06:46Z` "Tests passed" — "1 passed 41 passed" for the single-file run [40 existing + 1 new]; `2026-08-26T08:07:18Z` "Tests failed" — "1 failed 14 passed 1 failed 429 passed" for the full-suite run). The one failure is `test/healthz.real.test.ts` (ECONNREFUSED on port 3000, rule 13a's real-path test requiring a live dev server, none running for this pass) — unchanged from the Phase 15 baseline pattern, not a regression introduced this session.
- `tsc --noEmit` re-verified fresh by this writer at `2026-08-26T08:09:43Z`, exit 0 — after the last edit to the touched files, so this is live corroboration rather than a carried-forward claim.
- Known untested areas: none newly introduced; pre-existing deferrals (transition edit-in-place, usage checks, hidden date property) unchanged from Phase 14/15.

---

**Progressive update**: Session completed 2026-08-26 03:09

## Activity Log (auto-captured)
```
[08:06:46] TEST: Tests passed — 1 passed 41 passed
[08:07:18] TEST: Tests failed — 1 failed 14 passed 1 failed 429 passed
[08:10:34] EDIT: File written — docs/context/session-20260826-0249-main.md
[08:12:52] EDIT: File written — .commit-files
[08:12:58] EDIT: File written — .commit-msg
[08:13:01] GIT: Git operation — bash $HOME/.claude/scripts/git-commit.sh
```
